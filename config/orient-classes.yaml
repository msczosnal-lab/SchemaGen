# Klasy uczone w wielu orientacjach + grupa obrotow. Silnik: train/orient.py.
# group: D4 (8: obroty+lustra) | C4 (4 obroty) | C2 (0/180).
# exemplar_dir: katalog z <klasa>_r0.png (+ _m0 dla D4). OSOBNY folder per klasa.
classes:
  mostek:
    group: D4
    exemplar_dir: data/mostek_exemplars

min_score: 0.55
tile:
  size: 96
  margin: 8
