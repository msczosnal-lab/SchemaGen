# Prompt startowy — 010 labeler bbox-first + paleta

> Wklej w **nową sesję** [claude.ai/code](https://claude.ai/code) po `Start-GitSync.cmd Claude`.

```
SchemaGen — Builder. Aktywne zadanie: prompt 010.

Wczytaj:
1. sync/KOLEJNE-ZADANIE.md
2. sync/filip-to-zw.md (najnowszy wpis)
3. sync/prompts/010-labeler-bbox-first-palette.md
4. docs/claude-cowork-instructions.md

Etap 1: detekcja ELEMENTÓW na schemacie. Labeler: najpierw bbox, potem wybór hasła z config/symbol-palette.yaml.
NIE: kurator QET, cropy atlasu, multi-class YOLO, line tracer.

Po kodzie: pytest backend/tests labeler/tests → sync/zw-to-filip.md → commit-message.txt = [Claude] labeler: bbox-first + symbol palette (prompt 010)
```

Skrót: **kolejne zadanie**
