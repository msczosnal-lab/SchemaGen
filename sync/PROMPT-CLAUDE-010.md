# Prompt startowy — 010 labeler bbox-first + paleta

> Wklej w **nową sesję** [claude.ai/code](https://claude.ai/code) po `Start-GitSync.cmd Claude`.

```
SchemaGen — Builder. Aktywne zadanie: prompt 010.

Wczytaj:
1. sync/KOLEJNE-ZADANIE.md
2. sync/filip-to-zw.md
3. docs/schematic-interpretation.md
4. sync/prompts/010-labeler-bbox-first-palette.md

Filar 1/3: symbole graficzne. Bbox-first + config/symbol-palette.yaml (hasła PL, bez atlasu QET).
NIE: backend/atlas/, QET, kurator, OCR, line tracer, GraphBuilder w tej sesji.

Po kodzie: pytest backend/tests labeler/tests → sync/zw-to-filip.md → [Claude] labeler: bbox-first + symbol palette (prompt 010)
```

Skrót: **kolejne zadanie**
