# Prompt startowy — 002 OCR (filar tekst)

> Wklej w **nową sesję** [claude.ai/code](https://claude.ai/code) po `Start-GitSync.cmd Claude`.

**Kontekst 2026-06-24:** YOLO multi-class (`symbols_atomic_v1/v2`) działa. ContextResolver (wiersze Y) w Cursor — bez zmian w OCR.

```
SchemaGen — Builder. Aktywne zadanie: prompt 002-ocr-engine.

Wczytaj:
1. sync/KOLEJNE-ZADANIE.md
2. sync/filip-to-zw.md
3. docs/schematic-interpretation.md
4. sync/prompts/002-ocr-engine.md

Filar 2/3: TEKST na schemacie. PaddleOCR offline — backend/recognize/ocr_engine.py.
NIE: atlas QET, labeler (010 DONE), line tracer (osobna sesja — PROMPT-CLAUDE-002-LINES.md).

Po kodzie: pytest backend/tests labeler/tests → sync/zw-to-filip.md → [Claude] recognize: PaddleOCR engine (prompt 002-ocr)
```

Skrót: **kolejne zadanie**
